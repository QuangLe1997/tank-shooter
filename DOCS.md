# TANK SHOOTER — Tài liệu game (Game Bắn Xe Tăng)

> Đọc file này là hiểu toàn bộ game + biết chỗ nào để sửa, **không cần scan lại `index.html`**. Mục tiêu: 1 file duy nhất [`index.html`](index.html), zero-build.
>
> ✅ **Trạng thái: HOÀN THÀNH (P1–P9) + REWORK v2 (R1–R7) + TINH CHỈNH v3/v4 + VISUAL OVERHAUL v4 (G1–G5).** Game chơi trọn vẹn, live tại https://quangle1997.github.io/tank-shooter/. Kịch bản ở **[`GAME_DESIGN.md`](GAME_DESIGN.md)**. File này theo dõi bản đồ code (§0) + lịch sử (§14). **Toàn bộ chữ trong game là tiếng Anh; icon là bộ SVG tự vẽ (không emoji).**

### Rework v2 (R1–R7) — hệ thống hiện hành
| Mục | Trạng thái | Ở đâu |
|---|---|---|
| Xe tăng **không bao giờ chồng nhau** (tách cứng + hất văng) | ✅ | `separateTanks()` |
| **Súng kiểu mới**: băng đạn + nạp đạn (3-4s) + mảnh vỡ + auto-fire | ✅ | `GUNS`, `fireGun()`, `startReload()` |
| **Auto-aim** (mobile nòng tự khoá địch gần nhất) · desktop ngắm chuột | ✅ | `nearestTarget()`, `updatePlayer` |
| **Kho súng**: bắt đầu 1 súng cơ bản, nhặt thêm, đổi liên tục (1-8/cuộn/nút) | ✅ | `p.guns[]`, `giveGun/selectGun/cycleGun`, HUD `#gunlist` |
| **Buff theo thời gian (≤15s)**: ⚡bắn nhanh 💥sát thương 🏃tốc độ 📦băng đạn ∞vô hạn | ✅ | `POWERUPS`, `applyPowerup()`, `p.buffUntil`, `#buffbar` |
| Đạn nhiều → di chuyển chậm hơn xíu | ✅ | `updatePlayer` (heavy factor) |
| **Địa hình**: tường gạch (phá được) + khối thép (chắn) — cản xe & đạn | ✅ | `buildBlocks()`, `damageBlock()`, va chạm trong `updateBullets`+`separateTanks` |
| Xe tăng đẹp/hoành tráng hơn + camera thấp/chân thực hơn | ✅ | `buildTank()`, `updateCamera()` |
| Địch dễ hơn cho người mới, khó tăng dần theo màn | ✅ | `diffScale()`, `buildWaveQueue`, fire scaling |

**8 súng** (tier 1→3): rifle · smg · shotgun · flak · cannon · minigun · missile · railgun — mỗi súng = bộ thông số (dmg/fireCd/mag/reload/frags/spread/bspeed). Sửa cân bằng → chỉ chỉnh `GUNS`.

### Tinh chỉnh v3 (T1–T5)
| Mục | Ở đâu |
|---|---|
| **Phạm vi tác chiến**: vòng tròn mờ quanh player; chỉ bắn khi địch vào vùng; đạn chỉ tới trong vùng; mở theo màn + buff 🎯 + nâng cấp | `playerRange()`, `rangeRing`, gate trong `updatePlayer`, `maxDist` đạn |
| **Nổ xe hoành tráng** (flash+cầu lửa+mảnh văng+cột khói) + impact theo cỡ đạn | `tankExplode()` |
| **Vật phẩm** nhấp nháy + icon riêng từng loại + vòng đếm ngược (hết → biến mất) | `dropPowerup/updatePickups`, `emojiTex()` |
| **Đánh boss**: player **to bằng boss** (radius động) + tông húc văng nhau, không xuyên qua | `p.scale/p.radius`, `separateTanks` boss |
| **Khoảng cách min** giữa mọi xe (không dính sát) | `separateTanks` (GAP=0.7) |
| **Địa hình mê cung** dạng I/L/U/T/Z để ẩn nấp | `MAZE_SHAPES`, `buildBlocks()` |

### Visual overhaul v4 (G1–G5) — đánh bóng giao diện
| Mục | Ở đâu |
|---|---|
| **Toàn bộ chữ → tiếng Anh** (HUD, menu, tên súng/phép/buff/nâng cấp/boss/biome, banner) | `GUNS/SPELLS/POWERUPS/UPGRADES/BOSS_DEFS/BIOMES`, HTML overlay, `showBanner` |
| **Skin xe tăng chân thực**: texture tôn thép (panel/rivet/xước) + tread; PBR bắt phản chiếu; chạy gầm chi tiết; nòng có ống nhiệt + hút khói + hãm đầu nòng; tier `detail` (player+boss) thêm giỏ sau tháp/ống phóng khói/MG/đinh tán/móc kéo/ống xả | `panelTex()/trackTex()`, `buildTank({detail})` |
| **Camera mở màn điện ảnh**: hero thấp khoe nòng → vòng quanh kiểu duyệt binh → toàn cảnh → khớp đúng khung gameplay; letterbox + hạ-tiêu-đề + nút bỏ qua; ẩn HUD khi diễn; bỏ qua bằng input | `startIntro/updateIntro/endIntro`, biến `intro`, mode `'intro'` |
| **Bộ icon SVG tự vẽ (bỏ hết emoji)**: 33 glyph line-art quân sự; HUD/menu/nâng cấp dùng chung; badge tròn cho vật phẩm rơi | `GLYPH`, `iconEl/setIcon/initIcons`, `svgBadgeTex()` |
| **HUD/menu theme nhất quán + icon chuẩn** cho súng/đạn/máu/căn cứ/tốc/phép; vị trí gọn dễ quan sát; màu HP đỏ · căn cứ hổ phách · sẵn-sàng xanh · nạp-đạn đỏ | CSS `.ic/.gunicon/#weaponchip/.buffchip/.upcard`, `updateGunHUD/updateSpellHUD/updateBuffHUD` |
| **Mobile (chỉ ngang)**: 1 joystick lái (nòng tự ngắm + tự bắn → bỏ cần ngắm/nút bắn); nút **SWAP** (hiện súng đang dùng) / **DASH** / **SKILL** có nhãn; HP/căn cứ lên góc trái khỏi vùng ngón cái; menu thu gọn; cổng xoay máy khi cầm dọc + tự pause | `onTouchStart/Move/End` (`move`), `body.touch` CSS, `#rotate`, `checkOrientation()` |
| **Vòng đếm nạp đạn** nổi trên xe (arc quét + số giây + flash ✓ xanh + âm khi xong) | `drawReloadDial()`, `updateReload()`, `reloadSprite` |
| **Con trỏ tâm ngắm "+"** tùy biến (desktop) | CSS `body:not(.touch) #app/canvas{cursor:url(svg)}` |
| **Vũ khí GIỮ NGUYÊN qua màn** (chỉ reset khi chơi mới/thua/NG+); kho súng hiện khi có >1 súng | `chooseUpgrade`/`killBoss` không đụng `p.guns`; `updateGunHUD` toggle `#gunlist` |
| **Màn chuyển hồi hộp**: STAGE CLEAR → STAGE N → biome → tagline + vignette + scan line + âm dồn | `playStageTransition()`, `#stagewipe`, `STAGE_TAGS` |
| **Joystick mobile** (lắng nghe chạm trên `window` vì `#touch` là pointer-events:none) + **Fullscreen** (nút bật/tắt; tự bật + khoá ngang khi deploy) | `onTouchStart/Move/End` trên `window`, `reqFs/toggleFullscreen`, `#fsBtn` |
| **Desktop bắn chủ động**: bấm/giữ chuột trái để bắn (giới hạn theo `fireCd` súng), bỏ auto-fire; chuột phải = phép | `mousedown/up` → `S.firing`, gate bắn trong `updatePlayer` |
| **Radar/minimap** (góc phải): toàn cảnh arena — core, địch, boss, player (mũi hướng) + vòng tầm | `drawRadar()`, `#radar` canvas |
| **Lưu điểm cao (localStorage)**: BEST trên menu + "NEW BEST" khi phá kỷ lục | `BEST`, `recordRun/refreshBestLine`, `#bestline/#go-best` |
| **Cảnh báo CĂN CỨ**: viền đỏ nhấp nháy + "CORE UNDER ATTACK" + còi khi core <35% | `updateCoreAlarm()`, `#corealarm/#corewarn`, `body.corelow` |
| **Vật phẩm DRONE**: phi cơ bay quanh xe, tự ngắm + bắn địch ~22s | `spawnDrone/updateDrone`, POWERUPS `drone` |
| **Banner key-art AI** (gpt-image-2): màn hình mở đầu desktop (ngang) + cổng xoay máy mobile (dọc) | `assets/banner-desktop.jpg` (#start bg), `assets/banner-mobile.jpg` (#rotate bg) |
| **Vệt đạn phát sáng** (tracer additive) — thấy rõ đường đạn, to hơn trên mobile | `trailPool/addTrail/updateTrails`, `BVIS` |
| **HUD mobile nhỏ + trong suốt**; camera gần hơn (xe to hơn) trên mobile | `body.touch` CSS (`.chip` alpha thấp, topbar nhỏ), `camOff = MOBILE?27:33` |
| **Màn mở đầu cao cấp**: nút DEPLOY vàng đặc + badge BEST·STAGE, bỏ hộp mờ, hint nhẹ trên nền banner | `#start` CSS override (ẩn `.title`, `.panel` trong suốt) |
| **Mobile 2 chế độ bắn**: cò súng cố định góc trái (giữ=liên thanh, chạm=1 phát) + **AUTO** toggle (localStorage); joystick giới hạn nửa phải; nòng auto-ngắm | `#mFire/#autoBtn`, `autoFire`, `onTouchStart` clientX>34% |
| **Vệt đạn màu lửa**: vàng-cam (player) / đỏ-cam (địch) | `addTrail` color theo `owner` |
| **Trúng đạn vật lý**: xe địch + player rung (jolt) + bị đẩy lùi theo hướng đạn (xung lực giảm dần, boss/xe nặng cản hơn) | `kbx/kbz/hitT` (apply `updateEnemies/updatePlayer`, set `updateBullets`) |
| **Âm thanh riêng từng súng** (`shoot_<id>`, liên thanh = tick ngắn) + tiếng nổ xe địch dày (boom+clang+mảnh) | `sfx` switch |
| **Tượng đài Core VIP** (bệ tầng + pha lê + vòng năng lượng quay + cột sáng + vòm trường-lực + tia lấp lánh + mảnh pha lê) — **mỗi màn 1 biến thể** | `buildCore(stage)/rebuildCore/updateCore`, `CORE_THEMES` |
| **Fix địch dán sát = bắn trượt** + min-standoff (địch giữ khoảng cách, không dán, dễ ngắm) | đạn xuất phát mép thân `od=p.radius+0.6`; `PLAYER_STANDOFF=2.0` trong `separateTanks`; ram range nới |
| **Nạp đạn theo từng khẩu** (nạp nền độc lập, đổi súng khi đang nạp vẫn OK) | `inst.rU/rD`; `updateReload` hoàn tất MỌI khẩu; `selectGun` không reset |
| **SIEGE ROCKET**: lốp tên lửa vòng cung → ngòi nổ 2s (telegraph) → nổ xoá sạch địch bán kính 15; 5s/quả · 10 quả · nạp 30s · giữ vĩnh viễn | `fireRocket/updateRockets/updateFuses`, GUNS `rocket` (special) |
| **CRYO** (đóng băng địch khi trúng) + **TESLA** (sét lan tối đa 3 địch gần) | bullet `freeze`/`chain`/`chainR` trong va chạm |
| **Súng rơi theo màn**: tier thấp sớm, mạnh/mới (missile/tesla/railgun/rocket) từ màn 3 | `gunDropPool(stage)` |
| **Trạng thái nạp đạn từng khẩu** trên kho súng: ✓ xanh khi sẵn sàng, vòng tối quét (conic) khi đang nạp | `.gcool/.gchk`, `updateGunStatus` |
| **Vùng sát thương tên lửa**: vòng đỏ trên đất = đúng bán kính diệt (nhấp nháy 2s), nổ rung mạnh + shockwave | `makeDangerZone`, `updateFuses` (k clamp) |
| **Nòng pháo đổi theo súng**: 11 kiểu nòng riêng (gatling xoay, ống tên lửa, ray railgun phát sáng…) — nhìn xe biết đang dùng súng gì | `makeGunBarrel/setPlayerBarrel`, swap trong `updateGunHUD` |
| **Đạn vòng cung** (mục tiêu xa → cong, trực diện → thẳng; va chạm vẫn 2D) | `arcDist`→`vy`/`by`, `ARC_G=110`, tích phân trọng lực trong `updateBullets`; trail theo `by` |
| **Xe chết tan tành (chân thực)**: mảnh kim loại **hình dạng lộn xộn**, **nóng→nguội dần**, văng/xoay/**nảy thấp + nằm yên + mờ dần**; khói đen tức thì; xe to → nổ phụ + nhiều mảnh | `debris/spawnDebris/updateDebris`, `tankExplode` |

---

## 0. TRẠNG THÁI TÍNH NĂNG (đọc cái này trước)

Cột "Ở đâu" = tên hàm / marker để `grep`. Trạng thái: ✅ xong · 🚧 đang làm · ⬜ chưa làm.

### Khung dự án (đã có)
| Mục | Trạng thái | Ở đâu |
|---|---|---|
| Repo + GitHub Pages | ✅ | `main`/root → https://quangle1997.github.io/tank-shooter/ |
| Single-file zero-build (Three.js CDN) | ✅ | `index.html` (`<script type="module">`) |
| Kịch bản game đầy đủ | ✅ | `GAME_DESIGN.md` |
| Tài liệu + luật dự án | ✅ | `DOCS.md`, `CLAUDE.md` |

### Tiến độ build (theo phase trong GAME_DESIGN §14)
| Phase | Nội dung | Trạng thái | Ở đâu |
|---|---|---|---|
| **P1** | Engine + player tank (move/aim 360°/dash i-frame) + camera follow + twin-stick mobile + HUD skeleton | ✅ | `CONFIG/STATE/THREE SETUP/ARENA/PLAYER TANK/INPUT/UPDATE/MAIN LOOP` |
| **P2** | Bắn (Cannon) + đạn pool + va chạm + hit flash + damage/float text + muzzle light + shockwave ring | ✅ | `PROJECTILES`, `fireWeapon()`, `FX` |
| **P3** | 3 loại địch (grunt/scout/gunner) + AI + wave spawner + Core (căn cứ) bảo vệ + score/combo + chết/mạng/respawn + game-over | ✅ | `ENEMIES`, `WAVES`, `CORE`, `SCORE/COMBO`, `PLAYER DAMAGE` |
| **P4** | Juice: particle pool (sparks/smoke/debris) cho nổ/muzzle/hit/dash + screen shake + bloom; Audio: synth SFX (bắn/nổ/trúng/đau/core/wave/dash) + nhạc nền procedural 128 BPM (kick+bass+lead+hat), resume trong gesture | ✅ | `PARTICLES`, `AUDIO`, `bigExplode()`, `renderSong()` |
| **P5** | 6 vũ khí (đại bác/liên thanh/súng hoa/railgun/tên lửa dò/flak AoE) đổi bằng 1-6 hoặc cuộn/nút; 6 phép (tăng tốc/khiên/EMP/sửa chữa/không kích/ngưng đọng) cast E/nút + cooldown; power-up rơi (⭐💣⏱🛡❤️🔧) + nam châm hút + comeback-bias; weaponLvl nâng cấp | ✅ | `WEAPONS`, `SPELLS`, `POWERUPS`, `fireWeapon()`, `trySpell()`, `dropPowerup()` |
| **P6** | Khung boss đa pha + Boss S1 **Mãnh Ngưu** (húc telegraph + quạt/đạn vòng, pha 2 khi <50% HP) + thanh máu boss + wave→boss (sau đợt 5) + slow-mo khi hạ + lên màn | ✅ | `BOSS_DEFS`, `startBoss/updateBoss/killBoss()`, `bossSpread()` |
| **P7** | 5 biome (sa mạc/đô thị/băng/công xưởng/thành trì — đổi bg/fog/đất/đèn) + **màn nâng cấp roguelite (chọn 1/3 thẻ)** sau mỗi boss + 3 loại địch mới (mortar telegraph/shield chắn trước/bomber tự nổ) + wave theo màn + Victory + NG+ | ✅ | `BIOMES`, `applyBiome()`, `UPGRADES`, `openUpgrade/chooseUpgrade()`, `victory()`, mortar/shield/bomber trong `ENEMY_DEFS`/`updateEnemies` |
| **P8** | 5 boss phân hoá: Mãnh Ngưu (húc), Phong Bão (xoáy ốc+tên lửa dò+triệu quân), Cự Thần (đạn vòng+dậm đất+mìn), Tử Thần (laser quét+dịch chuyển+triệu), Bá Vương (tổng hợp+cuồng nộ) + Victory + NG+ | ✅ | `bossAttack()`, `groundPound/bossMine/bossMissiles/bossSummon`, laser trong `updateBoss` |
| **P9** | Auto-aim assist (mobile) + nút tắt tiếng + adaptive FPS (tự giảm độ phân giải khi lag) + cân bằng độ khó + gỡ dev-cheat | ✅ | `updatePlayer` (aim assist), `setMuted()`, `_fpsAcc` trong `loop()` |

### Điều khiển đã chạy (P1)
- Desktop: WASD/mũi tên di chuyển · chuột ngắm (raycast → mặt đất) · Space lướt (dash + i-frame, cooldown) · Esc/P pause.
- Mobile: nửa trái = joystick di chuyển (origin động nơi chạm) · nửa phải = joystick ngắm twin-stick · nút 💨 Dash / 🪄 Phép · ⏸ pause. `body.touch` tự bật.

---

## 1. Tổng quan & Context

- **Thể loại:** Game bắn xe tăng (tank shooter). Góc nhìn / 2D-3D: **chưa chốt** — sẽ quyết theo mô tả.
- **Mục tiêu (dự kiến):** điều khiển xe tăng, bắn hạ mục tiêu/địch, sống sót & ghi điểm.
- **Triết lý kế thừa từ neon-serpent-3d:** 1 file tự chạy, zero-build, không chói, chữ rõ, mobile-friendly.

### Tech stack (zero build — TBD chi tiết)
- Hiện tại: **Canvas 2D thuần** (không thư viện) cho nền placeholder.
- Có thể nâng lên Three.js (qua importmap CDN) nếu chọn hướng 3D — quyết sau.
- Âm thanh: dự kiến Web Audio API synth (không file ngoài).
- Lưu trữ: `localStorage`.

### Chạy / Deploy
- **Chạy qua HTTP server** (tránh chặn module ở `file://`):
  - `cd tank-shooter` → `python3 -m http.server 8765` → mở `http://localhost:8765/index.html`.
- **GitHub Pages:** bật sẵn từ nhánh `main`, thư mục `/` → **https://quangle1997.github.io/tank-shooter/** (tự cập nhật mỗi lần push `main`).
- Repo: `github.com/QuangLe1997/tank-shooter` (public).

---

## 2. Cấu trúc file `index.html` (hiện tại)

| Vùng | Nội dung |
|---|---|
| `<style>` | Theme tông quân sự (xanh olive `--green`, hổ phách `--amber`, thép `--steel`), title card, badge "Đang phát triển", responsive + safe-area |
| HTML body | `#bg` (canvas nền), `.stage` (title + subtitle + badge), `.hint` |
| `<script>` IIFE | nền canvas động: `resize()`, `drawGrid()`, `drawTank()`, vòng lặp `frame()` |

> Khi dựng gameplay thật, đề xuất tách rõ các vùng bằng comment `// ----- TÊN -----` giống neon-serpent-3d (CONFIG / STATE / SETUP / INPUT / UPDATE / RENDER / LOOP / UI) để dễ điều hướng.

---

## 3. Vòng đời / State machine (dự kiến)

`menu → playing → (paused) → gameover → menu/playing` — **sẽ chi tiết khi có gameplay.**

---

## 4. Gameplay & quy tắc
_(chờ mô tả)_

## 5. Cấp độ / Độ khó
_(chờ mô tả)_

## 6. Điểm / Combo / Phần thưởng
_(chờ mô tả)_

## 7. Vật phẩm / Power-up
_(chờ mô tả)_

## 8. Hiệu ứng / Âm thanh
_(chờ mô tả)_

---

## 9. Quy ước thiết kế (giữ khi sửa)
- File phải **tự chạy** (zero-build); không thêm bước build/đóng gói.
- Mobile-friendly: viewport `viewport-fit=cover`, dùng `env(safe-area-inset-*)`.
- Bảng màu quân sự: olive `--green #7fae3a`, amber `--amber #e7b54a`, steel `--steel #9bb0c0`, danger `--danger #e0603a`, nền `#0c1108`.
- Không chói; chữ tương phản cao; UI panel kính tối.
- Tham khảo pattern (HUD chip, joystick, audio synth, cosmic FX…) ở repo anh em **neon-serpent-3d**.

---

## 10. State (sẽ định nghĩa object `S` khi có gameplay)
_(chờ mô tả)_

## 11. localStorage keys (dự kiến)
| key | ý nghĩa |
|---|---|
| `tank_best` | điểm kỷ lục (dự kiến) |

## 12. Điều khiển (dự kiến)
- **Desktop:** WASD/mũi tên di chuyển · chuột ngắm/bắn (hoặc Space) — **chốt sau**.
- **Mobile:** joystick + nút bắn — **chốt sau**.

---

## 13. Cách test nhanh
- `python3 -m http.server 8765` rồi mở Chrome local; test tự động qua **Playwright MCP** (`browser_navigate`/`browser_evaluate`/`browser_take_screenshot`, `browser_resize` giả mobile 390×844 / desktop 1280×800).
- Kiểm cú pháp: trích `<script>` ra `.mjs` rồi `node --check`.

---

## 14. Lịch sử cập nhật lớn
- **2026-05-31 · NÚT ĐIỀU KHIỂN CAMERA MOBILE — ZOOM + XOAY VIEW (Z9):** (phản hồi: camera mobile quá gần, không thấy hết tầm bắn; xin nút bên trái để chỉnh view). Thêm cụm **#camCtl** (2 nút mờ, cạnh trái — ngay dưới cột HUD, trên DASH): **ZOOM** (cycle 1.0→1.5→2.0→2.6×, nhãn hiện mức, lưu `localStorage`) co/giãn `camOff` theo `S.zoom`; **ROTATE** xoay view 45°/lần (`S.camYaw`). Mặc định mobile zoom **1.5×** (kéo lùi sẵn để thấy tầm bắn). `updateCamera` nhân `camOff×zoom` rồi `applyAxisAngle(Y, camYaw)`; `getMoveDir` xoay input joystick theo `+camYaw` → **joystick luôn theo hướng màn hình** dù view đã xoay. Dời nút **AUTO** sang cụm tiện ích góc trên-phải (bolt, sáng khi bật) để nhường chỗ cạnh trái. Verify (Playwright @844×390): zoom kéo cam y 28→41; cam xoay đúng (yaw0 ở +z sau lưng → yaw90 sang +x); nút không đè DASH/FIRE; AUTO không đè FS; 0 lỗi console.
- **2026-05-31 · ĐỊCH CHẬM & NÉ ĐƯỢC ĐẠN (Z8):** (phản hồi: địch tấn công/di chuyển nhanh quá, hầu như không né được đạn). Nguyên nhân chính: đạn bay nhanh hơn xe + địch **bắn đón đầu (lead theo `p.vel`)** + ngắm chuẩn dần về stage 5 → đạn bám người. Giảm: **tốc độ đạn** grunt 30→24, gunner 34→26, shield 32→25 (xấp xỉ tốc độ xe → né ngang được); **tốc độ di chuyển** grunt 7→6, scout 15→12, gunner 6→5, shield 6→5, mortar 5→4, bomber 13→10; **bắn chậm hơn** grunt fireCd 1.7→2.0, gunner 0.34→0.46 + burst 4→3 + reload 1.8→2.3, shield 1.6→1.9; **giảm đón đầu** (chỉ từ ds>0.6, lead cap 0.5→0.3 ×0.55) + **giữ độ lệch ngắm cả late-game** (`(1-ds*0.6)*0.5`) + nhịp `rateMul` 2.0→2.2. Verify (Playwright): test né — đứng yên mất 5 HP/6s với 1 gunner, **đi ngang né sạch (0 HP)**; chỉ số mới đã load (gunner speed 5 / bspeed 26 / fireCd 0.46 / burst 3); 0 lỗi console.
- **2026-05-31 · FIX RENDER MOBILE NHỎ HƠN MÀN HÌNH (Z7):** (lỗi: trên mobile đôi lúc canvas render nhỏ hơn màn, hay gặp ở khúc intro→play). Nguyên nhân: `renderer.setSize(W,H)` mặc định ghi **inline style px** lên canvas; viewport mobile đổi SAU khi load/deploy (ẩn thanh địa chỉ, fullscreen, khoá xoay) mà resize bị lỡ/sai → canvas kẹt ở kích thước cũ nhỏ hơn (viền đen). Fix: (1) CSS `#app,canvas{width:100%;height:100%}` + đổi tất cả `setSize(W,H)`→**`setSize(W,H,false)`** → canvas **luôn lấp đầy bằng CSS**, buffer set riêng (không còn ghi px style). (2) Hàm **`doResize()`** (camera.aspect + renderer + composer + bloom) gọi khi: `resize`, `orientationchange` (delay 250+650ms), **`fullscreenchange`/`webkitfullscreenchange`**, `visualViewport.resize`, **cuối intro (`endIntro`)** và **sau deploy (300+800ms)** → bắt đúng lúc viewport mobile ổn định. Verify (Playwright): canvas fill 844×390 & 1024×560 (fills=true, bufferMatches=true, không inline px); qua intro→play vẫn fill 100%; 0 lỗi console. Hook `_resize`.
- **2026-05-31 · ĐÈN HIỆU VẬT PHẨM ẨN (Z6):** (yêu cầu: thấy được vật phẩm ẩn trong xe tăng / tường gạch để chủ động bắn trước mà nhận, thay vì phải phá mới biết). Trước đây brick/xe **random** rơi đồ lúc bị phá (không báo trước). Giờ **đánh dấu sẵn lúc tạo**: ~13% brick (không phải khối thép) + ~12% xe tăng **giấu 1 power-up**, và gắn **đèn hiệu (`lootBeacon`)** — viên ngọc vàng xoay + quầng sáng + vòng nhẫn + cột sáng — nổi trên đầu, **+ blip vàng trên radar** → người chơi **thấy & ưu tiên bắn** mục tiêu đó. Phá brick/diệt xe có dấu → **chắc chắn rơi** đúng power-up (`if(b.loot)/if(e.loot) dropPowerup`). Beacon tự gỡ khi chủ bị phá (`updateLootBeacons` theo dõi owner còn trong `blocks`/`enemies`); dọn khi sang màn/chơi mới. Verify (Playwright): 1 brick + 1 xe được đánh dấu, ảnh xác nhận đèn hiệu vàng nổi trên brick, diệt xe có dấu **rơi power-up (freeze)**; 0 lỗi console.
- **2026-05-31 · LÍNH TRÊN NÓC XE TĂNG (Z5):** (yêu cầu: từ màn 3 xe có thêm 3 lính đứng trên nóc auto bắn; máu/sức mạnh riêng, nâng cấp được; tầm nhỏ hơn xe; bắn chậm; chết là mất; vật phẩm tăng lính). Thêm hệ **RIDER** (`riders[]`): (1) **mở khoá ở màn 3** — `deployRiders(3)` khi vào stage 3 (`chooseUpgrade`, cờ `ridersUnlocked`, **giữ qua màn**, không tự hồi sinh). (2) Lính **ngồi trên nóc xe**: mỗi frame đặt vị trí = `player.pos + offset-mái-quay-theo-facing × (p.scale·TANK_SCALE)` (4 chốt trên nóc), scale theo xe; auto-ngắm địch gần nhất trong **tầm 14 (< tầm xe)** + bắn đạn owner 'p' **nhịp chậm** (fireCd 0.9). (3) **Máu riêng từng lính** (~24–40) + **thanh máu billboard trên đầu**; nâng cấp chung qua `soldierLvl` (armory). (4) **Phơi mình**: đạn địch trúng xe có **45% làm bị thương 1 lính** + bị **bào mòn khi xe bị vây** (5+stage mỗi 0.7s) → **chết là mất hẳn**. (5) Vật phẩm **REINFORCEMENTS** giờ bù **cả lính canh trụ lẫn lính trên xe** (về cap + hồi máu). Verify (Playwright): 3 lính bám nóc xe (dist ~1.2, cao 1.8), thanh máu hiện, maxHP khác nhau [32,31,36], **auto-bắn (địch 32→16)**, ~49 FPS lúc combat dày, 0 lỗi console. Hook `_dbg.riders`,`_deployRiders`. **(Cũng xác nhận DRONE auto-bắn OK** — test: drone bắn địch 32→16; lý do hay không thấy là vật phẩm random hiếm rơi + cache trình duyệt.)
- **2026-05-31 · BINH LÍNH ĐỒNG MINH CANH TRỤ (Z4):** (yêu cầu: thêm tối đa 5 lính cùng phe đứng canh tượng đài, tự bắn xe tăng địch; lính mạnh hơn xe nhưng nhiều xe sẽ chết; chiến đến chết; vật phẩm tăng lính + tăng súng cho lính). Thêm hệ **SOLDIER**: (1) **đội hình ≤5 lính** đứng vòng quanh Core (bán kính 6.8), mô hình bộ binh low-poly (mũ sắt, áo giáp, ba lô, súng trường); **auto-ngắm địch gần nhất trong tầm 30 + bắn** (đạn owner 'p' → tính điểm/combo cho player). (2) **Lính chặn đạn giúp Core** (đạn địch trúng lính trước) + **địch hành quân tới Core sẽ NHẮM/đánh lính chắn đường** (sửa target trong `updateEnemies`: nếu không đuổi player thì nhắm lính gần nhất trong 36u, mới tới Core). (3) **Cân bằng**: lính HP 60, sát thương 15 @0.5s (~30 dps > xe) → **1 lính thắng 1 xe & sống sót**; nhưng **đám đông áp sát = chết nhanh** (sát thương cận chiến `(7+stage·1.6)·số-xe-kề` mỗi 0.55s + hỏa lực tập trung) — verify: 1 xe→lính sống (3/3), **bầy 6 scout cận chiến→xóa sổ (3→0)**. (4) **Chiến đến chết** (không tự hồi sinh; FX tử trận). (5) **Tái triển khai 3 lính mỗi màn** (giữ cấp nâng súng `soldierLvl`). (6) **2 vật phẩm**: `troops` (REINFORCEMENTS — full đội 5 + hồi máu) & `armory` (TROOPS ARMED — `soldierLvl++` → +35% dmg & bắn nhanh hơn). Verify (Playwright): triển khai 3 lính vòng quanh Core, 1v1 sống / bầy đàn chết, power-up đúng (→5, lvl++), **60 FPS**, 0 lỗi console (trừ favicon). Hook debug `_dbg.soldiers`,`_deploy`,`_armory`. **(tinh chỉnh)** phóng to lính (`SOLDIER.scale=1.5`, ringR 6.8→7.4, body-block 0.72→0.95). **(Z4b — yêu cầu: thanh máu trên đầu, máu khác nhau, tầm vừa phải, lính BỰ, đi qua lại):** mỗi lính có **thanh máu billboard nổi trên đầu** (xanh→cam→đỏ theo %, hướng về camera); **máu ngẫu nhiên mỗi người** (~48–96, "mỗi người khác nhau"); **tầm bắn vừa phải** 30→**22**; **to hơn nữa** scale 1.5→**1.9** (ringR→7.8, body-block→1.15); **đi lảng vảng quanh chốt** (wander: chọn waypoint ngẫu nhiên trong ±1.9 quanh `home`, ease tới, quay mặt theo hướng đi khi không bắn). Verify: 5 thanh máu hiện, maxHP khác nhau [62,79,80,83,72], di chuyển được, scale 1.9, 60 FPS. **(Z4c — phản hồi "lính mạnh quá khó thua"):** GIẢM sức mạnh lính để Core còn rủi ro thật — `dmg 15→8`, `fireCd 0.5→0.7` (DPS ~30→~11, **−47%**), `hp 60→44` (**−27%**, variance hẹp lại ~36–58), `range 22→17`; nâng cấp armory nhẹ lại (dmg +0.35→+0.22, fire +0.12→+0.08). Verify (stage 3, bầy 6 gunner): lính giờ **thương vong thật (5→3)** thay vì bất tử, vẫn hỗ trợ tốt nhưng không còn là tường thành.
- **2026-05-31 · SKIN RẰN RI + ZOOM GẦN + KHÓI/NỔ SÚNG/INTRO XỊN (Z3):** (yêu cầu: thêm khói đẹp, intro xịn hơn, hiệu ứng nổ súng, zoom gần xe, skin kim loại rằn ri). (1) **Camo rằn ri**: thêm các vệt loang bất quy tắc (1 tông sáng + 2 tông tối, chồng lớp) vào albedo `_makePanelTex` → khi tô màu theo phe đọc thành **ngụy trang disruptive** (xanh olive cho player, đỏ/xanh… cho địch). (2) **Zoom gần xe** (`camOff` desktop 33/28→**28/23**, mobile 21/17.5→**18/14.5**) → xe to & chi tiết hơn. (3) **Khói đẹp hơn**: texture **cuộn mây mềm** (`smokeTex` — nhiều blob mềm chồng nhau) thay đốm radial cứng; `spawnPart` đổi map GLOW↔SMOKE theo `o.smoke` (chỉ `needsUpdate` khi đổi) + xoay ngẫu nhiên cho đỡ lặp. (4) **Nổ súng**: `muzzleFlash` mạnh hơn — lõi trắng nóng + lóe màu súng + 3 tia lửa bắn tới + làn khói nòng nhẹ (40%) + đèn chớp sáng hơn. (5) **Intro xịn**: đổi quỹ đạo camera (**cận cảnh thấp khoe nòng → lượn quanh → vươn cao kiểu duyệt binh → hạ về khung gameplay**), dài 5.2→**6.0s**, + **bụi chiến trường** bay quanh xe lúc intro. Verify (Playwright): ảnh cận cảnh thấy rõ **camo rằn ri**; gameplay camera y33→**26** (gần hơn); bắn ra tia lửa + lóe; intro hero-shot cận cảnh nòng + letterbox + title; 0 lỗi console (trừ favicon).
- **2026-05-31 · MẢNH VỠ KIM LOẠI CHÁY XÉ TOẠC + NỔ SIÊU MẠNH + ZOOM XE MOBILE (Z2):** (phản hồi: mảnh vỡ vẫn như "khối lập phương đổi màu" chưa chân thực; cần nổ siêu mạnh, tung toé, cháy rực; xe mobile bé quá). (1) **Làm lại wreck cho giống KIM LOẠI XÉ TOẠC**: bộ geometry mới = **tấm giáp mỏng, mảnh nhọn (Tetra/Octahedron jagged, flatShading), thanh gãy, bánh, nòng** thay cho box bo tròn; **16 mảnh** (trước 11); **màu CHÁY ĐEN xém** (`_SCORCH`) cho đa số, chỉ ~vài mảnh giữ **vệt sơn cháy tối** (palette ×0.45) — hết cảm giác "cube đổi màu"; bỏ panel-texture (dùng kim loại trơn cháy); scale **non-uniform** mỗi mảnh → méo mó. (2) **Nổ siêu mạnh**: lực phóng **rất lớn** (sp 10–24, up 11–22 ×√scale, spin ±20) → văng tung toé xa; fireball to hơn + **tia mảnh bắn xa** + **shockwave kép** + **rung mạnh** (cap 3.2→4.8) + đèn chớp sáng (5.2→7.5); mỗi mảnh **cháy rực**: vệt lửa + **tàn lửa (ember) bắn xa** + khói. (3) **Cháy rụi biến mất** (life 1.9–3.5s). Tăng particle pool **460→640** để cả vụ nổ lẫn vệt cháy đủ hạt. (4) **Zoom camera mobile** (`camOff` y27→**21**, z23.5→**17.5**) → xe tăng **to & ngầu hơn hẳn** trên màn nhỏ (desktop giữ nguyên 33/28). Verify (Playwright): wreck = mảnh đen xém/đỏ cháy jagged (ảnh xác nhận, hết cube), 16 mảnh, freeze-frame rõ tấm giáp + mảnh nhọn; smoke-test combat dồn dập **50 FPS** (peak 92 mảnh ~6 xác cùng lúc; chơi thường 60), pool không cạn, 0 lỗi console (trừ favicon). **(tinh chỉnh Z2b — phản hồi "đừng lố, vừa phải, nhanh, k che tầm nhìn"):** hạ cường độ về **vừa phải** (burst/shake 4.8→3.0 giảm ~½); **số mảnh theo cỡ xe** (`count` = 8 nhỏ / 11 / 14 / 16 boss — xe nhỏ nổ nhỏ); **mọi thứ tan NHANH**: fireball life 0.6→0.32s, khói 1.1→0.5s, cột khói 6→3 puff life 1.4→0.6s, mảnh life 1.9–3.5→**1.3–2.2s**, vệt lửa dừng ở 0.85s → **không lưu khói che tầm nhìn**; lực phóng 10–24→8–18 (vẫn tung toé). Verify: nhỏ 8 mảnh / lớn 14, debris sạch sau ~2.2s, ảnh xác nhận k còn mây khói chắn màn. **(tinh chỉnh Z2c — phản hồi "vẫn chưa thấy mảnh vỡ real lúc chơi"):** trước đó mảnh cứ **phát sáng cam ~1.9s** (trông như tia lửa, k giống kim loại) rồi biến mất quá nhanh → sửa: **nguội cực nhanh về KIM LOẠI ĐEN XÉM** (emissive cool 0.8→1.7/s, ~1s là tắt) + **mảnh TO hơn** (×1.15–1.7) + **nằm lại lâu** thành **xác wreckage thấy rõ trên mặt đất** (life 1.3–2.2→**2.8–4.3s**) rồi mới mờ; khói/lửa vẫn tan nhanh (k che màn). Pool 128→170. **Đã xác nhận live GitHub có code mới** (grep markers khớp). Verify (ảnh ở camera chơi thật): quanh xác xe thấy rõ **tấm giáp + mảnh nhọn + thanh/nòng đen xém** rải trên cát; **60 FPS** (peak 57 mảnh nằm), 0 lỗi console.
- **2026-05-31 · BỐ TRÍ LẠI ĐIỀU KHIỂN MOBILE + FULLSCREEN iOS (Z1):** (phản hồi: fullscreen k chạy iOS Chrome; nút phải che map; chuyển DASH sang trái trên cò bắn; fullscreen/sound/auto/pause cần trong suốt & gọn). (1) **iOS fullscreen**: feature-detect `FS_SUPPORTED` (documentElement.requestFullscreen|webkit) — iOS Safari/Chrome **không có** API → **ẩn hẳn nút fullscreen** thay vì để nút chết (deploy auto-fs vẫn no-op an toàn). (2) **DASH dời sang tay trái**, ngay **trên cò bắn** (`#mFire`); cụm trái xếp dọc từ dưới: **FIRE → DASH → AUTO**; tách `#mDash` khỏi `#mbtns` (giờ `#mbtns` chỉ còn **SWAP + SKILL**). (3) nút phải **nhỏ lại (58px) + bán trong suốt** (alpha .32/.42) → **không che map** nữa. (4) **PAUSE gom về góc trên-phải** chung với **FULLSCREEN + SOUND** thành cụm icon nhỏ; cả 3 **trong suốt 100%** (bỏ nền chip/viền/shadow, chỉ còn icon + drop-shadow), thứ tự mép→trong = mute · pause · fullscreen (fullscreen ngoài cùng → ẩn trên iOS không để lại khe hở). (5) **AUTO trong suốt** (nền mờ .22, bật = viền/chữ vàng phát sáng). topbar dời về sát mép trái (pause đã rời góc trái). Verify (Playwright 844×390): cụm phải-trên bg `rgba(0,0,0,0)`; DASH ở trái (L22) trên FIRE; SWAP/SKILL 2 nút trong suốt góc phải; 0 lỗi console (trừ favicon).
- **2026-05-31 · XE VỠ THÀNH BỘ PHẬN THẬT KHI CHẾT (Y4):** (phản hồi: tan vỡ đang là khối lập phương, muốn vỡ thành **các bộ phận của xe**). Thêm hệ **WRECK**: khi 1 xe chết, nó bung ra **11 mảnh là CÁC BỘ PHẬN nhận diện được** — tháp pháo, **nòng pháo**, tấm giáp thân, **đoạn xích**, **bánh đỡ**, cupola — **tô màu theo palette của chính xe đó** (địch đỏ/xanh…, player olive) + **kích thước theo cỡ xe thật** (`mesh.scale`). Dùng lại physics debris có sẵn (văng, xoay, nảy thấp, **nóng→nguội**, nằm đất rồi mờ); pool riêng **84 mesh** (gán geometry theo loại bộ phận), recycle đúng pool (`d.pool`), floor theo cỡ mảnh. `tankExplode(pos,color,scale,pal,ms)` — có `pal` thì bung bộ phận, không thì vẫn dùng khối hộp cũ (tường/khối vỡ). `killEnemy` truyền `e.def.pal`+`e.mesh.scale.x`; player truyền `PLAYER_PAL`; **boss bung mảnh khổng lồ** theo `boss.def.pal`. **Tiện thể FIX bug tiềm ẩn**: `killBoss` tham chiếu `boss.def.pal` trong `setTimeout` **sau khi `boss=null`** (sẽ throw khi boss chết) → đã capture `bpal/bms` vào local trước. Verify (Playwright, hook `_wreck/_tankExplode`): 11 mảnh/xe đúng bộ phận + đúng màu palette (đỏ grunt, xanh shield), văng/xoay tự nhiên, recycle đúng, 0 lỗi console (trừ favicon). Thêm hook debug `_tankExplode`,`_wreck`. **(nâng cấp)** mảnh **bay tung toé lực mạnh** (sp 7–17, up 9.5–18.5 ×√scale, spin ±15), **bốc cháy** (emissive 1.0–1.7 + mỗi mảnh **kéo vệt lửa cam/vàng + khói** trong `updateDebris` khi `age<1.3s`), rồi **cháy rụi biến mất** (life 1.8–3.2s, fade hết — không nằm lại đất). Verify: 11 mảnh bay cao 5.3 ở 0.17s, debris=0 sau 3.6s.
- **2026-05-31 · THANH MÁU GẮN THẲNG LÊN XE/TƯỢNG ĐÀI + MENU MOBILE TRONG SUỐT (Y3):** (phản hồi: mobile cần menu **trong suốt + cực gọn nhỏ** để thấy màn chơi bên dưới; **bỏ menu máu riêng** — gắn chart máu **thẳng lên xe tăng + tượng đài** cho dễ theo dõi). (1) **`#worldbars`**: 2 thanh máu nhỏ **bám theo unit theo từng frame** (`updateWorldBars` chiếu `worldToScreen`): thanh trên **xe player** (xanh→cam→đỏ theo %, kèm **×mạng**, nổi trên tháp pháo `y=3.7*scale`) + thanh trên **tượng đài Core** (cam→đỏ, `y=7.8`, có icon bunker nhỏ). (2) **Ẩn hẳn `#hpwrap`** (cụm máu góc cũ) ở **mọi nền tảng** — thông tin máu giờ nằm trên chính unit, **không cần menu riêng**. (3) **Mobile**: chip **trong suốt hơn nhiều** (alpha .82/.9 → **.26/.34**, blur 12→5px, shadow/nhạt viền giảm) → nhìn xuyên thấy chiến trường; cột trái **dồn sát lên** (hpwrap mất → weaponchip lên `+40`, gunlist `+66`) cho gọn nhất. Verify (Playwright, desktop 1280×800 + mobile 844×390): `#hpwrap` display:none; thanh player bám xe (55% + ×2), thanh core trên tượng đài (50%/45%), cả 2 ẩn khi unit chết/ngoài màn; **61 FPS**; HUD chip không tràn giữa (`overlapsCenter=[]`); 0 lỗi console (trừ favicon). **(bổ sung)** 2 thanh **nhấp nháy đỏ khi nguy kịch** (player ≤25%, core ≤30% — telegraph cảnh báo theo §2); smoke-test combat ở scale 1.2: bắn điểm-blank vẫn trúng (hugger chết, +1150 điểm), 60 FPS.
- **2026-05-31 · XE TĂNG TO HƠN + SKIN SIÊU CHÂN THỰC (Y2):** (phản hồi: xe hơi bé so với không gian + tăng độ chi tiết skin chân thực). (1) **`TANK_SCALE=1.2`** — phóng to **cả hình lẫn hitbox** của player + mọi địch lên +20% (boss giữ nguyên vì đã khổng lồ); áp dụng đồng bộ ở `makePlayer`/reset/ramp boss-parity (`p.radius=PLAYER.radius*p.scale*TANK_SCALE`, `mesh.scale=p.scale*TANK_SCALE`) + enemy (`r:1.5*def.scale*TANK_SCALE`, `mesh.scale=def.scale*TANK_SCALE`) → không lệch muzzle/va-chạm, không đổi số khó (speed/dmg/fireCd độc lập scale); nâng vòng đếm nạp đạn theo cỡ xe. (2) **PBR skin nâng cấp**: thêm **normal map sinh thủ tục** (height→Sobel: mạch ghép lõm, mối hàn & đinh tán nổi → giáp có **chiều sâu thật** dưới đèn động) + **roughness map** (sơn mờ vs kim loại mòn/xước **bóng lên**); albedo giàu hơn (vết sơn tróc lộ kim loại, **vệt gỉ chảy xuống**, mối hàn, ký hiệu chevron). Materials dùng chung → **mọi xe** (cả địch không-detail) đẹp hơn mà không tốn poly. (3) **Greeble chi tiết** (chỉ player+boss `detail:true`): **xích dự phòng vắt trên giáp nghiêng**, **cáp kéo cuộn bên hông** + móc, **can xăng** trên chắn bùn, **bánh xích dự phòng** buộc trên nóc, **xà chống lầy** ngang đuôi, **kính tiềm vọng** trên nóc tháp, **vành bảo vệ đèn pha**, **tay nắm hàn** hông tháp; hạ **dải màu phe** từ phát-sáng-neon → **sọc sơn mờ** (emissive .55→.16) cho chân thực. Verify (Playwright, desktop 1280×800, ảnh cận cảnh trước/sau qua hook `_view`): player scale 1.2 / radius 1.8, địch scale 1.2, **61 FPS**, greeble hiển thị đúng, 0 lỗi console (trừ favicon). Thêm hook debug `window._view` (camera/scene/renderer).
- **2026-05-31 · MOBILE HUD DỒN VỀ 2 MÉP — GIỮ TRỐNG GIỮA MÀN (Y1):** (phản hồi: màn mobile cần đẩy toàn bộ menu/điểm số về **trái & phải**, chừa **full giữa trên/dưới** để quan sát, info **nhỏ & gọn**, bớt rối che không gian). Bố cục lại CSS `body.touch`: **cột TRÁI (xuống dần)** = `#mPause` góc trên + `#topbar` (SCORE/STAGE/WAVE) cùng hàng → `#hpwrap` (máu+lõi+mạng) → `#weaponchip` (súng đang cầm) → `#gunlist` (kho súng + trạng thái ✓/nạp) **wrap nhiều hàng, `max-width:152px`** để không bao giờ tràn vào giữa dù đủ 11 súng; **cột PHẢI** = `#fsBtn/#muteBtn` góc → `#radar` → `#buffbar` (chuyển thành **cột dọc sát mép phải**, dưới radar) → cụm `#mbtns` (SWAP/DASH/SKILL); **cò bắn `#mFire` + `#autoBtn`** ở góc dưới-trái. `#topbar`/`#weaponchip`/`#gunlist`/`#buffbar` được gỡ căn-giữa (`transform:none`, bỏ `left:50%`), font/padding/icon thu nhỏ thêm. Kết quả: **dải giữa màn (cả trên lẫn dưới) hoàn toàn trống** cho tầm nhìn. Verify (Playwright, landscape 844×390, ép `body.touch`, **đủ 11 súng**): mọi phần tử HUD nằm trong mép trái (≤R202) hoặc mép phải (≥L728); `overlapsCenterTop=[]`, `overlapsCenterBot=[]`; ảnh chụp xác nhận giữa màn trống (xe/địch/lõi/COMBO đều thấy rõ); 0 lỗi console (trừ favicon).
- **2026-05-31 · TRẠNG THÁI SÚNG + NÒNG ĐỔI THEO SÚNG + VÙNG NỔ TÊN LỬA (X1–X3):** (X1) kho súng hiện **trạng thái từng khẩu** — dấu **✓ xanh** khi đã nạp xong/sẵn sàng, **vòng tối quét (conic-gradient)** khi đang nạp; cập nhật mỗi frame (`updateGunStatus`) nên thấy được tiến độ nạp 30s của tên lửa ngay cả khi đang cầm súng khác. (X2) tên lửa chạm đất hiện **vòng tròn đỏ trên mặt đất = đúng bán kính sẽ diệt địch** (đĩa mờ + viền đỏ nhấp nháy nhanh dần + bíp trong 2s), khi nổ **rung màn mạnh (shake +4.2) + shockwave** ra tới mép vùng; thêm **clamp** `k∈[0,1]` cho độ mờ (fix opacity âm). (X3) **nòng pháo xe tăng đổi theo súng đang cầm** — 11 kiểu nòng riêng: rifle nòng đơn, smg ngắn + băng, shotgun nòng to, flak loe, cannon dày-dài, **minigun 6 nòng gatling XOAY**, missile hộp phóng 4 ống, railgun 2 ray + cuộn phát sáng, cryo vòi băng phát sáng, tesla cuộn + cầu điện, rocket ống lớn; muzzle dời theo đầu nòng từng loại (`makeGunBarrel/setPlayerBarrel`, swap khi đổi súng). Verify: barrel khác nhau theo khẩu (muzzleZ/childCount/spin), status ✓/reloading đúng, vùng nổ đỏ opacity hợp lệ; 0 lỗi console.
- **2026-05-31 · SÚNG MỚI + NẠP ĐẠN THEO KHẨU (W1–W4):** (W1) **tách nạp đạn ra từng khẩu** (`inst.rU/rD` thay timer chung của player) → mỗi khẩu nạp **độc lập ở nền**; **đổi súng khác trong lúc khẩu kia đang nạp** vẫn được (khẩu đang nạp tự đầy đúng giờ dù đang cầm khẩu khác); đổi súng không còn huỷ nạp. (W2) **SIEGE ROCKET**: bắn 1 quả **lốp vòng cung** tới mục tiêu (auto-ngắm / điểm chuột), chạm đất cắm **ngòi nổ 2s** (vòng đỏ telegraph + bíp), rồi **nổ lớn xoá sạch mọi địch trong bán kính 15** (boss ăn 360, không one-shot); **5s/quả · băng 10 quả · nạp đầy 30s · giữ vĩnh viễn** sau khi nhặt; có mảnh vỡ + chùm nổ phụ. (W3) thêm **CRYO** (đạn đóng băng địch — set `stunUntil`) + **TESLA** (sét **lan sang tối đa 3 địch gần** trong bán kính 9). (W4) **rơi súng theo màn** (`gunDropPool`): smg/shotgun/cryo từ màn 1; flak/cannon/minigun từ màn 2; missile/tesla/railgun/rocket từ màn 3 — vẫn random trong nhóm. Verify (Playwright): nạp nền (rifle tự đầy 24 trong khi cầm smg/rocket) + đổi-khi-đang-nạp; rocket xoá 3 mục tiêu; cryo đóng băng; tesla lan 3; pool đúng theo màn; 0 lỗi console.
- **2026-05-31 · MẢNH VỠ CHÂN THỰC HƠN (phản hồi: nổ văng mảnh quá giả):** làm lại hệ debris — mảnh **kim loại hình dạng/kích thước lộn xộn** (scale non-uniform → tấm/thanh/cục, hết khối lập phương đều); **nóng đỏ lúc nổ rồi nguội dần thành kim loại tối** (emissiveIntensity → 0 trong ~1s); **nảy thấp (restitution 0.22) + ma sát không khí + nằm yên trên đất khi chậm** rồi **mờ dần (opacity)** thay vì co nhỏ; đa số màu kim loại tối, ~28% màu vỏ xe. Vụ nổ: thêm **khói đen ngay lập tức** + bớt tia lửa neon cho bớt kiểu "pháo hoa". Verify (đóng băng để xem): 1 xe → ~11 mảnh hình dạng lộn xộn, tối + vài mảnh màu vỏ/nóng; 0 lỗi console.
- **2026-05-31 · GIẢM ĐỘ KHÓ (phản hồi: địch di chuyển nhanh quá):** tốc độ di chuyển địch **↓ ~20%** (scout 21→15, bomber 17→13, grunt 9→7, gunner/shield 7→6, mortar 6→5); **tốc độ đạn địch ↓** cho dễ né (grunt 34→30, gunner 40→34, shield 36→32); **bắn chậm hơn** (`rateMul` base 1.7→2.0, fireCd các loại ↑ chút, dmg ↓ chút); **ít quái cùng lúc** (`cap` 6+stage·2 → 5+stage·1.5: s1=6, s5=12) + **spawn thưa hơn** (0.9→1.15s) + ít quái mỗi đợt ((stage-1)·2 → ·1.5); ram scout 16→11. Verify scout 21→15, grunt 9→7, chạy ok 0 lỗi.
- **2026-05-31 · ĐẠN VÒNG CUNG + XE CHẾT TAN TÀNH:** (1) **đạn bay vòng cung** khi mục tiêu xa, **thẳng** khi trực diện — đạn có quỹ đạo đạn đạo (`vy`/`by` + trọng lực `ARC_G=110` trong `updateBullets`), độ cong tỉ lệ khoảng cách tới mục tiêu (railgun nhanh cong ít, tên lửa chậm cong nhiều); **va chạm vẫn 2D x/z** nên gameplay không đổi (chỉ đẹp hơn); vệt lửa + viên đạn bay theo cung. Áp dụng cả đạn player (ngắm gần=thẳng, xa=cung — lấy khoảng cách tới mục tiêu khoá hoặc điểm ngắm chuột) lẫn đạn địch (theo khoảng cách tới mục tiêu). (2) **xe tăng chết tan tành**: thêm hệ **mảnh vỡ 3D** (pool 84 box văng ra theo vận tốc + trọng lực, xoay, nảy đất, cháy emissive rồi tắt) + lửa dày hơn (flash + 2 lớp fireball nóng/cam + tia lửa) + cột khói; **xe càng to effect càng đã** (scale≥2.2 và boss → vụ nổ phụ trễ 160ms + nhiều mảnh hơn; boss death văng cả đống mảnh theo `boss.r`); **knockback trúng đạn mạnh hơn** chút. Verify: bắn xa→arc(vy giảm dần), gần→thẳng; giết xe→12 mảnh; 0 lỗi console.
- **2026-05-31 · FIX: địch dán sát bắn không trúng + min-standoff:** đạn của player giờ **xuất phát từ mép thân xe** (`od = p.radius + 0.6`) thay vì đầu nòng (3.46 → ~5.9 khi xe phình to lúc đánh boss) → đạn **không còn bay vượt qua** xe địch đang dán sát (muzzle-flash vẫn ở đầu nòng cho đẹp). Thêm **`PLAYER_STANDOFF = 2.0`**: `separateTanks` đẩy địch ra giữ **khoảng cách tối thiểu** với xe player → địch **không dán nữa, dừng ở mức min**, dễ quan sát + ngắm bắn. Rammer (scout/grunt…) vẫn gây **sát thương va chạm** trong dải standoff (ram range = `e.r+p.radius+PLAYER_STANDOFF+1.5`). Boss vẫn brawl bình thường (code riêng, dùng GAP). Verify: scout từ glued(1.0)→giữ 6.18u; bắn trúng địch point-blank (grunt+scout đều mất máu); player vẫn ăn 32 ram dmg; 0 lỗi console.
- **2026-05-31 · HIT FEEL + AUDIO + CORE MONUMENT (Q1–Q3):** (Q1) **cảm giác trúng đạn chân thực** — xe địch khi trúng đạn bị **rung (jolt) + đẩy lùi theo hướng viên đạn** (xung lực giảm dần ~9/s; xe nặng/boss cản nhiều hơn theo `def.scale`); xe player trúng đạn cũng bị **đẩy nhẹ theo hướng đạn + screen shake** (`kbx/kbz/hitT`, set ở `updateBullets`, áp dụng ở `updateEnemies/updatePlayer`; bỏ qua khi đang bất tử). (Q2) **âm thanh**: mỗi súng 1 tiếng bắn riêng `shoot_<id>` (rifle/smg/shotgun/flak/cannon/minigun/missile/railgun — súng nhanh dùng tick rất ngắn để liên thanh nghe thành tràng), tiếng nổ xe địch dày hơn (boom + clang kim loại + mảnh vỡ). (Q3) **tượng đài Core VIP** thay reactor cũ: bệ nhiều tầng + viền phát sáng + **pha lê năng lượng** + **vòng năng lượng quay kiểu gyro** + **cột sáng dọc** + **vòm trường-lực** + **tia lấp lánh bay quanh** + **mảnh pha lê** (màn 3+); **mỗi màn 1 biến thể** theo `CORE_THEMES` (cyan→thép→băng→lửa→vàng; số vòng/tia/mảnh tăng dần). `rebuildCore(stage)` khi qua màn (giữ maxHP, hồi đầy). Verify core màn1 (cyan,2 vòng) + màn2 (thép,3 vòng), knockback gunner đẩy 2.68u, 0 lỗi console.
- **2026-05-31 · TITLE POLISH + MOBILE FIRE MODES:** (1) **màn mở đầu chuyên nghiệp** — bỏ hộp mờ + nút xanh muddy, thay bằng nút **DEPLOY vàng đặc** cao cấp + badge "★ BEST · STAGE" gọn + hint điều khiển mờ nhẹ trên nền banner (ẩn title CSS vì banner đã có); (2) **vệt đạn màu lửa** — vàng-cam (0xffb43a) cho đạn player, đỏ-cam (0xff5a24) cho địch (`addTrail` theo owner); (3) **mobile 2 chế độ bắn**: nút **cò súng cố định góc trái** (icon reticle, **giữ = liên thanh, chạm = 1 phát** — tiết kiệm đạn) + nút **AUTO** bật/tắt (lưu localStorage; AUTO = súng tự bắn như cũ; ẩn cò khi AUTO bật); **joystick giới hạn nửa phải màn** (clientX>34%) để tay trái giữ cò; **nòng luôn auto-ngắm địch gần nhất**. Default = MANUAL. Verify desktop menu + mobile layout + AUTO toggle (lưu localStorage), 0 lỗi console.
- **2026-05-31 · MOBILE FEEL & AI BANNER:** (1) **HUD top mobile nhỏ lại + trong suốt** (`.chip` alpha .4–.48, topbar val 17→14px, HP bar mỏng) để thấy chiến trường bên dưới; (2) **camera gần hơn trên mobile** (`camOff = MOBILE?27:33` + intro handoff khớp) → xe tăng to hơn trên màn nhỏ; (3) **vệt đạn phát sáng** (tracer sprite additive, pool 240) cho cả đạn mình & địch — đường đạn rõ/sáng, đạn to hơn trên mobile (`BVIS=1.6`); (4) **banner key-art AI** sinh bằng media-tools (gpt-image-2): ảnh ngang "STEEL SIEGE / 3D TANK SHOOTER" làm nền màn mở đầu desktop, ảnh dọc làm nền cổng xoay máy mobile (`assets/banner-*.jpg`, tối ưu JPEG ~0.5MB). Verify desktop menu + portrait gate + landscape HUD, 0 lỗi console.
- **2026-05-31 · AWARENESS & PROGRESSION (đánh giá & nâng cấp):** thêm **radar/minimap** (toàn cảnh arena: core/địch/boss/player + vòng tầm, góc phải); **lưu điểm cao** bằng localStorage — hiện BEST trên menu + "★ NEW BEST ★" khi phá kỷ lục (kỷ lục giữ qua các lần chơi); **cảnh báo căn cứ** (viền đỏ nhấp nháy + "CORE UNDER ATTACK" + còi khi core <35% → tránh chết oan); **vật phẩm mới DRONE** (phi cơ bay quanh xe, tự ngắm + bắn địch ~22s). Sửa radar canvas bị `#app,canvas{inset:0}` đẩy sang trái → ghim đúng top-right; dời cảnh báo khỏi vùng combo. Thêm hook debug `_powerup`. Verify desktop, 0 lỗi console.
- **2026-05-31 · CONTROLS FIX & FULLSCREEN (O1–O3):** (O1) **sửa joystick mobile không hiện** — listener gắn nhầm trên `#touch` (pointer-events:none) nên không nhận chạm; chuyển lắng nghe trên `window` (nút con `stopPropagation` nên không kích nhầm joystick), chỉ chạy khi đang chơi → cần lái xuất hiện đúng chỗ chạm + knob theo ngón (test bằng TouchEvent tổng hợp). (O2) **chế độ toàn màn hình**: nút bật/tắt cạnh loa + tự bật & khoá ngang khi DEPLOY trên mobile (iPhone Safari không có API → no-op nhẹ nhàng). (O3) **desktop bắn chủ động**: BỎ auto-fire; **bấm = 1 phát, giữ chuột = bắn liên tục** nhưng luôn bị giới hạn theo `fireCd` của súng (test: 0.8s giữ rifle = 4 viên); chuột phải = phép. Verify desktop 0 lỗi console.
- **2026-05-31 · COMBAT & TRANSITION POLISH (N1–N4):** (N1) **vòng đếm ngược nạp đạn** phát sáng nổi trên xe — arc quét từ 12h + số giây ở giữa + flash dấu ✓ xanh + âm "lên đạn / sẵn sàng" khi xong (`drawReloadDial/updateReload`, billboard `reloadSprite`, hoàn tất theo thời gian trong loop chứ không chờ bắn); (N2) **con trỏ desktop = tâm ngắm "+"** tùy biến (SVG data-URI, viền đen cho rõ trên mọi nền); (N3) xác nhận **vũ khí GIỮ NGUYÊN qua màn** (test thật boss-kill→upgrade→màn 2: rifle/cannon/railgun còn nguyên) — chỉ reset khi chơi mới/thua/NG+; hiện lại thanh kho súng (gọn) khi có >1 súng để thấy rõ; (N4) **màn chuyển hồi hộp** STAGE CLEAR → STAGE N → tên biome → tagline (`STAGE_TAGS`) + vignette + scan line + âm dồn dập (`playStageTransition`, `#stagewipe`). Thêm hook debug `_startBoss/_killBoss`. Verify desktop, 0 lỗi console.
- **2026-05-31 · MOBILE UX (chỉ hỗ trợ ngang):** **1 joystick** lái xe (origin động — chạm đâu hiện đó); nòng **tự ngắm địch gần nhất + tự bắn** nên bỏ cần ngắm phải + nút bắn (gỡ code `aimS`/`stickR` thừa). Nút **SWAP** (hiện icon súng đang dùng, chạm để đổi) / **DASH** / **SKILL** có nhãn rõ; ẩn thanh kho súng (`#gunlist`) + chỉ báo phím desktop (`#abilities`); dời HP/căn cứ lên góc trái khỏi vùng ngón cái; menu thu gọn cho màn ngang; joystick to hơn. **Chỉ landscape** — cổng `#rotate` “ROTATE YOUR DEVICE” khi cầm dọc + `checkOrientation()` tự pause. Help đổi theo nền tảng (`.desktop-only/.touch-only`). Verify portrait gate + landscape HUD, 0 lỗi console.
- **2026-05-31 · VISUAL OVERHAUL v4 (G1–G5):** (G1) dịch **toàn bộ** chữ sang tiếng Anh (tên game → STEEL SIEGE; HUD/menu/banner/tên súng-phép-buff-nâng-cấp-boss-biome); (G5) skin xe tăng chân thực hơn — texture tôn thép (panel/rivet/xước) + tread, PBR bắt phản chiếu môi trường, chạy gầm + nòng (ống nhiệt/hút khói/hãm đầu nòng) chi tiết, tier `detail` cho player+boss (giỏ sau tháp, ống phóng khói, MG, đinh tán, móc kéo, ống xả); (G4) **camera mở màn điện ảnh** (hero thấp khoe nòng → vòng quanh kiểu duyệt binh → toàn cảnh → khớp đúng khung gameplay; letterbox + hạ-tiêu-đề + nút skip; ẩn HUD khi diễn; bỏ qua bằng input sau grace); (G2/G3) **bộ icon SVG tự vẽ thay toàn bộ emoji** (33 glyph line-art quân sự) cho HUD/menu/vật phẩm/nâng cấp + theme nhất quán + vị trí tối ưu. Verify desktop 1280×800 + mobile 390×844, 0 lỗi console (trừ favicon 404).
- **2026-05-31 · Boss locomotion kiểu xe tăng:** boss chỉ tiến/lùi theo hướng thân; muốn tông phải **quay đầu xe về phía user** trước (telegraph để né) rồi mới lao thẳng theo hướng đã khoá (không bẻ lái giữa chừng → né được). State machine `aim → charge` trong `updateBoss`; nòng (turret) vẫn xoay theo user độc lập.
- **2026-05-31 · TINH CHỈNH v4 (U1–U3):** xe địch cũng có vùng tác chiến theo cỡ xe (er = def.range·r/1.5) — chỉ bắn khi mục tiêu trong vùng + đạn địch giới hạn tầm; vòng tầm player mảnh & nhạt hơn. Đổ bóng thật (PCF soft shadow, desktop) + đèn mạnh hơn → chân thực hơn. Buff lâu hơn (28s); 🎯 tầm bắn + 📦 băng đạn **vĩnh viễn (∞)**; nhặt trùng nhóm thì thay thế (không cộng dồn); HUD hiện ∞.
- **2026-05-31 · TINH CHỈNH v3 (T1–T5):** phạm vi tác chiến (vòng mờ + chỉ bắn khi địch trong vùng + đạn giới hạn tầm, mở theo màn/buff); nổ xe tăng hoành tráng + impact theo cỡ đạn; vật phẩm nhấp nháy + icon riêng + vòng đếm ngược; đánh boss player to bằng boss + tông húc văng nhau; min-gap giữa các xe; địa hình mê cung I/L/U/T/Z.
- **2026-05-31 · REWORK v2 (R1–R7):** sửa theo phản hồi sau khi chơi —
  (R1) xe tăng không còn dính/chồng vào nhau, va chạm hất văng;
  (R2) làm lại súng: băng đạn + nạp đạn + mảnh vỡ + **auto-fire**, mobile **auto-aim**, kho súng nhặt/đổi, bắt đầu 1 súng cơ bản;
  (R3) buff theo thời gian (bắn nhanh/sát thương/tốc độ/băng đạn/vô hạn) + rơi súng ngẫu nhiên;
  (R4) địa hình tường gạch phá được + khối thép chắn (cản xe & đạn);
  (R5/R6) mô hình xe tăng chi tiết hơn + camera thấp/chân thực hơn;
  (R7) địch nhẹ tay với người mới, khó tăng dần theo màn.
- **2026-05-31 · P9:** Auto-aim assist cho mobile (hút nhẹ về địch trong nón ngắm), nút tắt tiếng, adaptive FPS (tự hạ độ phân giải khi <45fps), cân bằng độ khó, gỡ dev-cheat. → Hoàn thành P1–P9.
- **2026-05-31 · P8:** 5 boss phân hoá hành vi (húc / xoáy ốc + tên lửa dò + triệu quân / đạn vòng + dậm đất shockwave + mìn / laser quét + dịch chuyển + triệu / tổng hợp cuồng nộ) + màn CHIẾN THẮNG + New Game+. Verify đủ 5 boss xuất hiện đúng thứ tự + victory.
- **2026-05-31 · P7:** 5 biome đổi theo màn (đất/nền/fog/đèn) + **màn chọn nâng cấp roguelite (1/3 thẻ)** sau mỗi boss (11 nâng cấp: máu/sát thương/nhịp bắn/tốc độ/dash/hút máu/chí mạng/phép/mạng/căn cứ/vũ khí) + 3 địch mới (mortar lốp đạn telegraph, shield chắn đòn trước, bomber lao tự nổ) + Victory + New Game+. (Terrain gạch/thép/băng: hoãn — sẽ thêm ở polish nếu cần.)
- **2026-05-31 · P6:** Khung boss đa pha + Boss S1 Mãnh Ngưu (húc có telegraph + bắn quạt/vòng, pha cuồng nộ <50% HP) + thanh máu, wave→boss sau đợt 5, slow-mo khi hạ, lên màn kế (boss scale theo màn; S2–S5 phân hoá ở P8).
- **2026-05-31 · P5:** 6 vũ khí (cannon/auto/shotgun/railgun/missile-homing/flak-AoE) + đổi súng (1-6/cuộn/nút) + weaponLvl; 6 phép (overdrive/aegis/EMP/repair/airstrike/timewarp) + cooldown; power-up rơi có nam châm hút + ưu tiên hồi máu khi nguy. HUD vũ khí/phép.
- **2026-05-31 · P4:** Juice (particle nổ/khói/spark + shake + bloom) + audio synth (SFX + nhạc nền 128 BPM).
- **2026-05-31 · P2+P3:** Combat core — Cannon + đạn pool + va chạm; 3 loại địch (grunt/scout/gunner) + AI đuổi/bắn/đâm; wave spawner ramp; **Core (căn cứ)** phải bảo vệ; score + combo (×8); player nhận sát thương/chết/mất mạng/respawn + game-over (HẾT MẠNG / CĂN CỨ BỊ PHÁ). Game đã chơi được.
- **2026-05-31 · P1:** Engine 3D (Three.js) + player tank (di chuyển, ngắm 360°, dash i-frame) + camera follow + twin-stick mobile + HUD skeleton + màn Title/Pause. Arena sa mạc.
- **2026-05-31:** khởi tạo repo + GitHub Pages + kịch bản `GAME_DESIGN.md` + luật `CLAUDE.md`.

> **Last updated:** 2026-05-31 · nhánh `main` · trạng thái: HOÀN THÀNH P1–P9 + R1–R7 + T1–T5 + Visual Overhaul v4 (G1–G5) + Mobile UX (1 joystick, landscape-only) + Combat/Transition polish (N1–N4) + Controls fix & Fullscreen (O1–O3) + Awareness & Progression (radar, high-score, core alarm, DRONE power-up) + Hit-feel/Audio/Core monument (Q1–Q3) + Súng mới & nạp-theo-khẩu (W1–W4) + Trạng thái súng/nòng đổi/vùng nổ (X1–X3) + Mobile HUD dồn 2 mép (Y1) + Xe to hơn & skin siêu chân thực (Y2) + Thanh máu gắn lên xe/tượng đài & menu mobile trong suốt (Y3) + Xe vỡ thành bộ phận thật khi chết (Y4) + Bố trí lại điều khiển mobile & fullscreen iOS (Z1) + Mảnh vỡ kim loại cháy + nổ siêu mạnh + zoom xe mobile (Z2) + Skin rằn ri + zoom gần + khói/nổ-súng/intro xịn (Z3) + Binh lính đồng minh canh trụ (Z4) + Lính trên nóc xe tăng (Z5) + Đèn hiệu vật phẩm ẩn (Z6) + Fix render mobile nhỏ hơn màn hình (Z7) + Địch chậm & né được đạn (Z8) + Nút camera mobile zoom/xoay-view (Z9). Toàn bộ chữ tiếng Anh, icon SVG tự vẽ.
